# examples

Examples in this directory are for validation and observation, not application features.

- `import_both.py` imports `codecpp` and `pyvista` in both orders.
- `show_runtime.py` prints the active VTK runtime origin from Python.
